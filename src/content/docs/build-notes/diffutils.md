---
title: diffutils
description: An opinionated Linux® distribution based on musl libc and toybox
---

- `./bootstrap` requires `gnulib` even when passing `--gen --force --no-bootstrap-sync --no-git --skip-po`
