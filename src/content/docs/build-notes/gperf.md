---
title: gperf
description: An opinionated Linux® distribution based on musl libc and toybox
---

## Prepare
- `autoreconf` requires `texi2pdf`
- `make install` breaks if `autoreconf` is run, use the provided `./autogen.sh` instead
