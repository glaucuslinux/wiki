---
title: less
description: An opinionated Linux® distribution based on musl libc and toybox
---

## Configure
- Prefer `posix` as the minimal `regex` implementation for `less` use case over `pcre2`
- Upstream should enable `--with-secure` by default
