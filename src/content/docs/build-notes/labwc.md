---
title: labwc
description: An opinionated Linux® distribution based on musl libc and toybox
---

- Depends on `librsvg` which depends on `rust`
- Runs certain software better than `weston` and `cage` which tend to break often; does not require `xwayland-satellite` to show client side decorations
